<nav class="navbar navbar-dark navbar-expand-lg bg-dark px-md-0 shadow-sm">
    <div class="container-fluid">
        <button class="navbar-toggler"
                type="button"
                data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent"
                aria-expanded="false"
                aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('app_home') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href=" {{ path('app_category_index') }}">Catégories</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href=" {{ path('app_article_new') }}">Nouvel article</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href=" {{ path('app_contact') }}">Nous contacter</a>
                </li>
                {% if is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <a class="nav-link" href=" {{ path('app_profile_show') }} " role="button">Profil</a>
                    </li>
                    <li>
                        <a class="nav-link" href=" {{ path('app_logout') }} " role="button">Déconnexion</a>
                    </li>
                {% else %}
                    <li>
                        <a class="nav-link" href=" {{ path('app_register') }} " role="button">S'enregistrer</a>
                    </li>
                    <li>
                        <a class="nav-link" href=" {{ path('app_login') }} " role="button">Connexion</a>
                    </li>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li>
                        <a class="nav-link" href=" {{ path('admin') }} " role="button">Administration</a>
                    </li>
                {% endif %}
            </ul>

            <form class="d-flex" role="search" action="{{ path('app_research') }}" method="GET">
                <input class="form-control me-2" type="search" name="search" placeholder="Votre recherche" aria-label="Search">
                <button class="btn btn-outline-success" type="submit">Chercher</button>
            </form>
        </div>
    </div>
</nav>