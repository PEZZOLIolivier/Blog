{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
    <h2 class="text-center">Liste des catégories</h2>
    <div class="mb-5">
        {% for category in homeCategory %}
            <div class="row">
                <div class="text-center fs-1 border rounded-3 m-2 p-2 bg-dark col">
                    <a class="btn btn-dark" href="{{ path('app_article_show', {'id': category.id}) }}">{{ category.name }}</a>
                </div>
            </div>
        {% endfor %}
    </div>

{% endblock %}
